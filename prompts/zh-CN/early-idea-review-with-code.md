下面是用户和另一个 coding agent 的对话上下文，包含用户需求和该 agent 给出的初步思路，不是最终方案：

```text
<PASTE_CONVERSATION_CONTEXT_HERE>
```

## 你要做的事

评审这个思路值不值得继续推进到正式方案阶段。你拥有同样的代码仓库访问权限，请实际读代码做评审，不要纯靠对话推理。

## 动作 0：先复述

从上面对话里提取并复述：

- 用户最初的原始诉求 / 原 bug 是什么，一句话。
- 当前思路的方向是什么，一句话。

如果对话里没说清原始诉求，直接指出来，不要硬猜。

## 动作 1：实读代码

打开对话中提到的文件，自己读一遍，验证 agent 自陈的代码现状是否准确。

同时用搜索工具找：

- 原 bug 直接相关的代码。
- 当前思路要触及但对话里没提到的代码。

阅读范围反向约束：只读三类代码。

1. 验证 agent 自陈现状的代码。
2. 原 bug 直接成因附近的代码。
3. 思路明显需要触及但 agent 没提到的代码。

不要为“全面评估”展开读整个模块。读完后列出实际打开过的文件路径。

## 当前阶段是思路对齐，不是方案评审

现在还没有正式方案、没有具体实现、没有 SQL、没有 API 形状。以下内容不在本轮评审范围：

- 边界条件。
- 上线后会挂在哪。
- 数据写入失败的并发场景。
- 任何“假设这样实现，那么会出现 X 问题”的预演。

这些问题在正式方案出来后再评。本轮提前评属于越位。

## 本轮评审四件事

1. 对题度：思路解决的是不是动作 0 里复述的原始诉求？有没有偏题、漏掉原问题、或扩张到原问题之外？
2. 实读代码 vs 对话自陈：逐条列出关键工程前提，标 `[对话+实读一致]`、`[对话+实读冲突]`、`[agent 没说我读后补充]`。
3. 是否有更小的路：不重新设计、只修原 bug 直接成因，能不能解决？如果说有，必须给出具体到文件:行号的修复点。
4. 路线比较：当前思路是不是当前项目里最合适的路线。替代路线必须解决同一个原始诉求，不扩张问题域，并基于实际读到的代码。

## 产品语义闸

如果讨论涉及多种产品语义，先列出当前思路采用的是哪种语义。之后每条问题必须标注它基于哪种语义成立。只在另一种语义下成立的问题不要列。

## 凑数标签

每条问题末尾必须标：`[核心]` / `[边缘]` / `[凑数]`。

如果论证里出现多个“如果……又如果……”的连环假设，多半是 `[凑数]`。

## 禁止行为

- 不要列只在恶意或反常使用路径下才成立的问题。
- 不要列当前系统既有取舍、本思路没加重的问题。
- 不要给“建议加测试 / 加日志 / 加注释 / 防御性代码 / 重构 / 优化 / 更严谨”这类没具体指向的话。
- 不要给具体修改代码。

## 输出格式

动作 0：原始诉求复述：[一句话] | 当前思路方向：[一句话]

动作 1：我实际打开过的文件：[文件路径列表]

然后选其一：

### A. 有阻塞性发现

对题度：[判断 + 一两句理由]

实读代码 vs 对话自陈：

- [前提] | 状态：[对话+实读一致 / 对话+实读冲突 / agent 没说我读后补充] | 文件:行号 | 简述

是否有更小的路：[有就给具体文件:行号；没有写“无”]

产品语义闸（如适用）：[当前选用语义]

关键问题（按重要性排序）：

1. [问题描述] | 基于的语义：X | 不解决会怎样：Y | [核心/边缘/凑数]

路线比较：

- 路线判断：[当前路线已合适 / 有更适合路线 / 仅有更小修法 / 无法判断]
- 更适合路线：[一句话；无则写“无”]
- 依据：[文件:行号 + 已有代码模式 / 调用关系 / 职责边界]
- 优于当前思路的原因：[一句话；无则写“无”]
- 缺少的信息：[仅“无法判断”时填写；其余写 N/A]

### B. 已审视无阻塞问题

对题度：[判断]

实读代码 vs 对话自陈：[逐条列状态，至少包含 1 条 `[对话+实读一致]` 的工程前提]

是否有更小的路：[有就给具体文件:行号；没有写“无”]

产品语义闸（如适用）：[当前选用语义]

已审视维度声明：我审视了以下维度，未发现阻塞性问题——[至少 3 个具体维度]

判断结论：思路可以推进到方案阶段。

路线比较：同 A。
