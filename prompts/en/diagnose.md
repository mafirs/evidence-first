Locate and read the code paths relevant to the issue. Explain which files you read, why they are relevant, and whether there may be omissions.

Do not modify, create, or delete files. Do not run commands that change project state. Do not include a full proposed patch. Allowed output: analysis, conclusions, reasoning, and evidence from existing code.

Requirements:

- Give a clear conclusion. If evidence points to one answer, say it directly. If multiple branches depend on runtime conditions, list those branches and their triggers.
- For every key reasoning step, cite evidence as file path + line number + a one-sentence summary of what the code does.
- Separate code facts from inference. Mark inference explicitly.
- If a conclusion depends on unread code, unverified assumptions, or external configuration, state that clearly.
